name: shreejikhakhra
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  #General Packages
  intl: ^0.19.0
  provider: ^6.1.2
  badges: ^3.1.2
  image_picker: ^1.1.2
  carousel_slider: ^5.1.1
  google_sign_in: ^6.2.1

  # Razorpay Payment Gateway
  razorpay_flutter: ^1.3.7

  # Firebase Dependencies
  firebase_core: ^3.1.1
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.2.1
  firebase_storage: ^12.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

  # App Icon Generator
  flutter_launcher_icons: ^0.13.1

#  App Icon Configuration
flutter_icons:
  android: true
  ios: true
  image_path: "lib/assets/images/SHREEJI_logo1.png"

# Flutter Configuration
flutter:
  uses-material-design: true

  assets:
    - lib/assets/images/
